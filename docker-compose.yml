services:
  ruby-app:
    build: .
    container_name: evidence_bot
    ports:
      - "3000:3000"
    volumes:
      - .:/rails
      - ./log:/rails/log
    environment:
      - RAILS_ENV=development
    tty: true
